# frontend/.env.example
# Local dev API (FastAPI)
VITE_API_BASE=http://127.0.0.1:8000

# Static mode (reads from /public/api/*.json)
# false for normal local API development, true for static demo hosting
VITE_USE_STATIC=false

# Optional fallback: when API mode is enabled and backend is unreachable,
# attempt static files in /public/api
VITE_API_TO_STATIC_FALLBACK=false

# Optional: embed a published Power BI dashboard in the /powerbi route
# Example: https://app.powerbi.com/view?r=...
VITE_POWERBI_EMBED_URL=
